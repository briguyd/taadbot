import { bot } from "../index";
import * as logger from "winston";

export function discordModule(constructor: Function) {
  //  return function(target: Function) {
  logger.info("decorator");
  //   bot.addModule(constructor);
  //  };
}
